import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    title:{
        fontSize:30,
        color: '#000',
        margin: 10
    },
    container:{
        alignItems: 'center',
    }
});
export default styles;