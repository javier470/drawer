import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    img: {
        width: 400, 
        height: 300,
    },
    title:{
        fontSize:30,
        color: '#000',
        margin: 10
    },
    container:{
        alignItems: 'center',
    }
});
export default styles;